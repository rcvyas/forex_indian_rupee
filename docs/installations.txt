step 0: open terminal

step 1: type in 'pip' in terminal

step 2: if it says 'The program 'pip' is currently not installed. You can install it by typing:sudo apt install python-pip:'
			goto step 3
		else:
			goto step 4
			
step 3: type in the following commands
		1. 'sudo apt-get update'
		2. 'sudo apt-get install python-pip'
		
step 4: Install pandas by typing in 'pip install pandas' if not already installed

step 5: Install numpy by typing in 'pip install numpy' if not already installed

step 6: Install scikit-learn by typing in 'pip install -U scikit-learn' if not already installed

step 7: Install scikit-learn by typing in 'pip install -U matplotlib' if not already installed

Now the installations are done, u may proceed to test the code.
